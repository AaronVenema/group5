<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/main.css">
    <script src="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/main.js"></script>
    <title>Document</title>
</head>
<body>
<div id="calendar"></div>

<div class="row">
    <form class="col" id="income">
        <input name="name" type="text" placeholder="name of income..." />
        <input name="amount" type="number" placeholder="$$$ rounded up..." />
        <input name="dateStr" type="date" />
        <button type="submit">Add Income</button>
    </form>
    <form class="col" id="expense">
        
    </form>
</div>

<script>
        document.addEventListener('DOMContentLoaded', function() {
var calendarEl = document.getElementById('calendar');
var calendar = new FullCalendar.Calendar(calendarEl, {
    initialView: 'dayGridMonth',
    selectable: true,
    eventClick: async function(event) {
        console.log(event.event._def) 
    },
    events: [
            // {
            //     id:"0",
            //     title: bill.name,
            //     start: bill.datecreated,
            //     ammount: bill.ammount,
            //     category: bill.category_id
            // }
        {
            id:"0",
            title: "groceries",
            start: "2022-10-28",
            ammount: 150,
            category: "food"
        }
    ]
});

calendar.render()

});
    </script>